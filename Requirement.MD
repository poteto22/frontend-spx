ต้องการทำ Front End Project สำหรับ control SPX-CG และสร้าง Rundown CG สำหรับใส่ข้อมูลต่างๆ
อ่าน API Document ใน 'spx_cg_api.md'
ตัวอย่าง Template ของ SPX อยู่ใน 'Example/'
ตัวอย่างหน้าตาของโปรแกรมอยู่ที่ 'Example/CG Web App.png'
เบื้องต้นวางแผนออกแบบ Front End Design ก่อน และให้เขียนเป็น Code มาให้เลย

สิ่งที่ต้องการในหน้าเว็บมีดังนี้

1. เชื่อมต่อกับ API ของ SPX และดึงข้อมูลมาแสดงในรูปแบบที่ user สามารถเลือกใช้ได้
2. สร้างหน้าเว็บสำหรับสร้าง Rundown CG โดยให้ user สามารถใส่ข้อมูลต่างๆ ได้ตามต้องการ

ทำงานโดยการใช้ API เพื่อทำให้ Template CG ทำงาน
http://localhost:5656/api/v1/item/play/mainbar
โดยเมื่อสั่งเล่นจาก API ด้านบน SPX จะมาเรียก http://localhost:8080/mainbar และทาง Frontend จะส่งข้อมูลที่ต้องการผ่าน API เป็น JSON มีรายยละเอียดดังนี้
{
"head": "หัวเรื่อง",
"topic": "มอบทุนศึกษา-อุปกรณ์กีฬา รร.ผลิตนักตบทีมชาติ",
"mainbar": "./assets/bar/MAIN BAR.png",
"headbar": "./assets/head/top-bar-1.png"
}
ต้องการให้สร้าง Front End Project เพื่อควบคุม SPX-CG ให้สามารถทำงานร่วมกันได้ โดยมีรายละเอียดดังนี้

1.  **เชื่อมต่อกับ SPX API**: โปรแกรมต้องสามารถเชื่อมต่อกับ SPX-CG ผ่าน API ได้ โดยปกติจะอยู่ที่ `http://localhost:5656/api/v1`
2.  **การแสดงผล**: เมื่อสั่งเล่น CG ผ่าน API (เช่น `http://localhost:5656/api/v1/item/play/mainbar`) SPX จะไปเรียกเว็บหน้าเว็บที่กำหนดไว้ (เช่น `http://localhost:8080/mainbar`) เพื่อรับข้อมูล CG
3.  **การสร้าง Rundown**:
    - ผู้ใช้สามารถสร้างรายการ CG (Rundown) ได้จากหน้าเว็บนี้
    - แต่ละรายการ CG จะประกอบด้วยข้อมูลพื้นฐานดังนี้:
      - `head`: หัวเรื่อง
      - `topic`: หัวข้อย่อย
      - `mainbar`: รูปภาพสำหรับ Main Bar (เช่น `./assets/bar/MAIN BAR.png`)
      - `headbar`: รูปภาพสำหรับ Head Bar (เช่น `./assets/head/top-bar-1.png`)
      - (อาจมีข้อมูลอื่นๆ ที่จำเป็นตามที่กำหนด)
4.  **การสั่งเล่น CG**:
    - ผู้ใช้สามารถกดปุ่มเพื่อสั่งเล่น CG แต่ละรายการได้
    - การสั่งเล่นจะส่งข้อมูล CG (JSON) ไปยัง SPX API ในรูปแบบที่กำหนด
5.  **เทคโนโลยี**:
    - ต้องการให้สร้างเป็นโปรเจกต์ Front End ที่สามารถทำงานได้ทันที
    - ควรใช้เทคโนโลยีที่ทันสมัยและเหมาะสม (เช่น React, Vue, หรือ Angular) โดยให้เลือกที่เหมาะสมที่สุดกับโจทย์
    - โค้ดต้องสะอาด เข้าใจง่าย และสามารถขยายความสามารถได้ในอนาคต

ต้องการให้ทำเป็นโปรเจกต์ที่พร้อมใช้งานได้เลย โดยให้วางโครงสร้างไฟล์ให้เหมาะสม มีการแยกโค้ดเป็นส่วนๆ และมีคำอธิบายประกอบโค้ดอย่างชัดเจน

หน้า Frontend มีการออกแบบดังนี้

1. หน้าจอแสดงผล Main Rundown
2. หน้าจอสร้าง Rundown
3. หน้าจอตั้งค่า
4. หน้าจอควบคุม Rundown
   Theme โทนสว่าง

ปรับเปลี่ยนหน้าตาสร้างรายการ CG ใหม่
เมื่อกดปุ่มสร้างรายการ CG ใหม่ ให้เปิดPopUp สำหรับกรอกข้อมูลรายการ โดยให้เริ่มจากการเลือกประเภทของ CG
โดยซ่อนส่วนอื่นๆไว้ก่อน แล้วจะค่อยๆ เพิ่มส่วนต่างๆ ในรายการ CG นั้นขึ้นมาตามลำดับ โดยจะสรุปขั้นตอนการทำงานของแต่ละประเภทของ CG ไว้ก่อน

ตัวอย่างการทำงานของแต่ละประเภทของ CG

1. Logo บาร์ (SPX ID = logo)
   - Logo (จำเป็นต้องมีข้อมูล) [json ID = logo] List เลือกรูปจาก asset/logo/

2. บาร์ประเด็น (SPX ID = mainbar)
   เมื่อเลือกอันนี้จะให้มีช่องให้กรอกข้อมูลดังนี้
   - Top Bar (เว้นว่างได้) [json ID = head]เพิ่มปุ่มสำหรับกดเพื่อใส่ข้อมูลที่ใช้บ่อย เช่น ประเด้นร้อน,สถานการณ์เด่น,สัมภาษณ์ทางโทรศัพท์ (สามารถเพิ่มได้จากหน้า config)
   - Topic (จำเป็นต้องมีข้อมูล) [json ID = topic]
   - Main Bar (จำเป็นต้องมีข้อมูล) [json ID = mainbar]
   - Top Bar Image (เว้นว่างได้) [json ID = headbar]
3. บาร์ 2 บรรทัด (SPX ID = bar2line)
   เมื่อเลือกอันนี้จะให้มีช่องให้กรอกข้อมูลดังนี้
   - Top Bar (เว้นว่างได้) [json ID = head]เพิ่มปุ่มสำหรับกดเพื่อใส่ข้อมูลที่ใช้บ่อย เช่น ประเด้นร้อน,สถานการณ์เด่น,สัมภาษณ์ทางโทรศัพท์ (สามารถเพิ่มได้จากหน้า config)
   - Line Top (จำเป็นต้องมีข้อมูล) [json ID = line1]
   - Line Botton (จำเป็นต้องมีข้อมูล) [json ID = line2]
   - Main Bar (จำเป็นต้องมีข้อมูล) [json ID = mainbar]
   - Top Bar Image (เว้นว่างได้) [json ID = headbar]

4. บาร์พิธีกร 2 คน (SPX ID = bar2name)
   เมื่อเลือกอันนี้จะให้มีช่องให้กรอกข้อมูลดังนี้
   - Top Bar (เว้นว่างได้) [json ID = head]เพิ่มปุ่มสำหรับกดเพื่อใส่ข้อมูลที่ใช้บ่อย เช่น ประเด้นร้อน,สถานการณ์เด่น,สัมภาษณ์ทางโทรศัพท์ (สามารถเพิ่มได้จากหน้า config)
   - Name Left (จำเป็นต้องมีข้อมูล) [json ID = name1]
   - Name Right (จำเป็นต้องมีข้อมูล) [json ID = name2]
   - ตำแหน่งพิธีกร (จำเป็นต้องมีข้อมูล) [json ID = line2]
   - Main Bar (จำเป็นต้องมีข้อมูล) [json ID = mainbar]
   - Top Bar Image (เว้นว่างได้) [json ID = headbar]
